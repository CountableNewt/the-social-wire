meta {
  name: Reconnect Operations Jetstream
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/v1/operations/ingestion/reconnect
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{oauthAccessToken}}
  DPoP: {{dpopProof}}
  Content-Type: application/json
}

body:json {
  { "auditNote": "Reconnect stalled Jetstream ingestion", "environmentConfirmation": "{{operationsEnvironmentConfirmation}}" }
}
