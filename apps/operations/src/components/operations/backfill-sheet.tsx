"use client"
import { useMutation } from "@tanstack/react-query"
import { AlertTriangle, Play } from "lucide-react"
import { useMemo, useState } from "react"
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"
import { AlertDialog, AlertDialogClose, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger } from "@/components/ui/alert-dialog"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Field, FieldDescription, FieldGroup, FieldLabel } from "@/components/ui/field"
import { Input, Textarea } from "@/components/ui/input"
import { Select } from "@/components/ui/select"
import { Sheet, SheetContent, SheetDescription, SheetFooter, SheetHeader, SheetTitle } from "@/components/ui/sheet"
import { useOperationsAuth } from "@/lib/auth-context"
import { dryRunBackfill, operationsRequest } from "@/lib/operations-api"
import { canQueueBackfill } from "@/lib/operations-policy"
import type { BackfillDryRun, EnvironmentName, Gap } from "@/lib/operations-types"

export function BackfillSheet({ gap, environment, open, onOpenChange }: { gap?: Gap; environment: EnvironmentName; open: boolean; onOpenChange: (open: boolean) => void }) {
  const { session } = useOperationsAuth()
  const [auditNote, setAuditNote] = useState("")
  const [reviewed, setReviewed] = useState(false)
  const [productionConfirmation, setProductionConfirmation] = useState("")
  const [sourceMode, setSourceMode] = useState<BackfillDryRun["sourceMode"]>("jetstream_replay")
  const request = useMemo<BackfillDryRun>(() => ({ gapId: gap?.id, sourceMode, startCursor: gap?.startCursor, endCursor: gap?.endCursor, collections: gap?.collections.length ? gap.collections : ["site.standard.document"], authorDids: [], batchSize: 1000, rateLimit: 500, maxConcurrency: 4 }), [gap, sourceMode])
  const dryRun = useMutation({ mutationFn: () => dryRunBackfill(session, request) })
  const changeSourceMode = (value: string) => {
    setSourceMode(value as BackfillDryRun["sourceMode"])
    dryRun.reset()
    setReviewed(false)
  }
  const create = useMutation({ mutationFn: () => operationsRequest(session, "/v1/operations/backfills", { method: "POST", body: JSON.stringify({ dryRun: request, expectedEstimate: dryRun.data?.estimatedCount, auditNote, environmentConfirmation: productionConfirmation || undefined }) }) })
  const canRun = canQueueBackfill({ dryRunComplete: Boolean(dryRun.data), reviewed, environment, environmentConfirmation: productionConfirmation, auditNote, pending: create.isPending })
  return <Sheet open={open} onOpenChange={onOpenChange}><SheetContent><SheetHeader><SheetTitle className="text-sm font-semibold">Backfill Gap</SheetTitle><SheetDescription className="mt-1 text-[10px] text-muted-foreground">Dry-run-first recovery with resumable checkpoints.</SheetDescription></SheetHeader><div className="flex-1 overflow-y-auto overscroll-contain p-4"><div className="flex items-center justify-between border-b pb-3 text-xs"><span>Gap <Badge tone="danger" className="ml-1">{gap?.status ?? "Open"}</Badge></span><span className="text-[10px] text-muted-foreground">{gap ? new Date(gap.detectedAt).toLocaleString() : "No gap selected"}</span></div><section className="mt-4"><p className="text-xs font-semibold">Cursor / Time Range (μs)</p><div className="mt-2 rounded-md border bg-muted/25 p-3 font-mono text-[10px]"><p>{gap?.startCursor ?? "—"} .. {gap?.endCursor ?? "—"}</p><p className="mt-2 text-muted-foreground">Δ {gap?.startCursor && gap?.endCursor ? (gap.endCursor - gap.startCursor).toLocaleString() : 0} μs</p></div></section><section className="mt-4"><div className="flex items-center justify-between"><p className="text-xs font-semibold">Dry-Run Summary</p><Button size="sm" variant="outline" onClick={() => dryRun.mutate()} disabled={!gap || dryRun.isPending}>{dryRun.isPending ? "Estimating…" : dryRun.data ? "Refresh Estimate" : "Run Dry-Run"}</Button></div><dl className="mt-2 divide-y rounded-md border text-[10px]"><Summary label="Estimated Events" value={dryRun.data?.estimatedCount.toLocaleString() ?? "Required"} /><Summary label="Estimated Duration" value={dryRun.data ? `~ ${Math.ceil(dryRun.data.estimatedDurationSeconds / 60)} min` : "—"} /><Summary label="Existing Conflicts" value={dryRun.data?.conflicts.length ? String(dryRun.data.conflicts.length) : "None"} /></dl></section><section className="mt-4"><p className="text-xs font-semibold">Backfill Configuration</p><FieldGroup className="mt-3 grid-cols-[1fr_1.2fr]"><FieldLabel>Source Mode</FieldLabel><Select ariaLabel="Source Mode" value={sourceMode} onValueChange={changeSourceMode} options={[{ value: "jetstream_replay", label: "Jetstream Replay" }, { value: "pds_reconciliation", label: "PDS Reconciliation" }]} /><FieldLabel>Start Time (μs)</FieldLabel><Input className="font-mono" value={request.startCursor ?? ""} readOnly /><FieldLabel>End Time (μs)</FieldLabel><Input className="font-mono" value={request.endCursor ?? ""} readOnly /><FieldLabel>Collection Filters</FieldLabel><div className="flex flex-wrap gap-1">{request.collections.slice(0, 2).map((item) => <Badge key={item}>{item}</Badge>)}{request.collections.length > 2 ? <Badge>+{request.collections.length - 2}</Badge> : null}</div><FieldLabel>Bounded Batch Size</FieldLabel><Input value={request.batchSize} readOnly /><FieldLabel>Rate Limit</FieldLabel><Input value={request.rateLimit} readOnly /><FieldLabel>Max Concurrency</FieldLabel><Input value={request.maxConcurrency} readOnly /><FieldLabel>Snapshot End Cursor</FieldLabel><Input className="font-mono" value={dryRun.data?.snapshotEndCursor ?? "Dry-run required"} readOnly /></FieldGroup></section><Field className="mt-4"><FieldLabel htmlFor="audit-note">Operator Audit Note</FieldLabel><Textarea id="audit-note" value={auditNote} maxLength={280} onChange={(event) => setAuditNote(event.target.value)} placeholder="Add an audit note for this backfill (required)" /><FieldDescription className="text-right">{auditNote.length} / 280</FieldDescription></Field><label className="mt-4 flex items-start gap-2 text-[10px]"><input type="checkbox" className="mt-0.5" checked={reviewed} onChange={(event) => setReviewed(event.target.checked)} /><span>I have reviewed the dry-run summary, understand the impact, and want to backfill this gap.</span></label>{environment === "production" ? <Field className="mt-3"><FieldLabel htmlFor="production-confirmation">Production Confirmation</FieldLabel><Input id="production-confirmation" value={productionConfirmation} onChange={(event) => setProductionConfirmation(event.target.value)} placeholder="Type PRODUCTION" /><FieldDescription>A second explicit environment confirmation is required.</FieldDescription></Field> : null}<Alert variant="warning" className="mt-4"><AlertTriangle className="mb-1 size-3.5" /><AlertTitle>Backfills can increase load</AlertTitle><AlertDescription>This action cannot be undone, but the job can be paused or cancelled.</AlertDescription></Alert>{create.isSuccess ? <Alert className="mt-3"><AlertTitle>Backfill Queued</AlertTitle><AlertDescription>The worker can now claim this recovery job.</AlertDescription></Alert> : null}</div><SheetFooter className="flex items-center justify-end gap-3"><Button variant="outline" onClick={() => onOpenChange(false)}>Cancel</Button><AlertDialog><AlertDialogTrigger render={<Button variant="destructive" disabled={!canRun} />}><Play /> Run Backfill</AlertDialogTrigger><AlertDialogContent><AlertDialogHeader><AlertDialogTitle className="text-sm font-semibold">Run This Backfill?</AlertDialogTitle><AlertDialogDescription className="mt-2 text-xs text-muted-foreground">The confirmed dry-run will be queued in {environment === "production" ? "Production" : "Development"}. This action is recorded in the immutable audit history.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogClose render={<Button variant="outline" />}>Cancel</AlertDialogClose><AlertDialogClose render={<Button variant="destructive" onClick={() => create.mutate()} />}>Queue Backfill</AlertDialogClose></AlertDialogFooter></AlertDialogContent></AlertDialog></SheetFooter></SheetContent></Sheet>
}

function Summary({ label, value }: { label: string; value: string }) { return <div className="flex justify-between px-3 py-2"><dt>{label}</dt><dd className="font-mono text-muted-foreground">{value}</dd></div> }
